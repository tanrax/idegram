{
  "private": true,
    "scripts": {
	"deploy": "gh-pages -d dist",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "github": "rm -r docs && npx nuxi generate && mv .output/public docs && touch docs/.nojekyll"
  },
  "devDependencies": {
    "nuxt": "^3.3.1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.12.1",
    "gh-pages": "^5.0.0"
  }
}
